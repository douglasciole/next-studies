"use client";
import React from "react";

const AddToCart = () => {
	return (
		<button onClick={() => console.log("Test")} className="btn btn-primary">
			Add to the cart
		</button>
	);
};

export default AddToCart;
